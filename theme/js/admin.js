/**
 * Admin JS functions
 */
(($) => {
	// Theme switcher functionality
	$(document).ready(() => {
		// This is just a placeholder for admin-side functionality
		// The actual theme switching happens in the frontend
	});
})(jQuery);
